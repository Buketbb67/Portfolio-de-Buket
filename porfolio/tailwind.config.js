  /** @type {import('tailwindcss').Config} */
 export default {
  content: ["./src/**/*.{js,jsx}",],
    theme: {
      extend: {
        /* rajouter un objet font-family et lier mes polices(nom dans le fichier css) à un nom de classe */
      },
    },
    plugins: [],
  }

/* Tailwind version 3. */